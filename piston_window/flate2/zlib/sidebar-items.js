initSidebarItems({"struct":[["DecoderReader","A ZLIB decoder, or decompressor.This structure implements a `Read` interface and takes a stream of compressed data as input, providing the decompressed data when read from."],["DecoderWriter","A ZLIB decoder, or decompressor.This structure implements a `Write` and will emit a stream of decompressed data when fed a stream of compressed data."],["EncoderReader","A ZLIB encoder, or compressor.This structure implements a `Read` interface and will read uncompressed data from an underlying stream and emit a stream of compressed data."],["EncoderWriter","A ZLIB encoder, or compressor.This structure implements a `Write` interface and takes a stream of uncompressed data, writing the compressed data to the wrapped writer."]]});