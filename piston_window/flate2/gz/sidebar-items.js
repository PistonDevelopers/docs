initSidebarItems({"struct":[["Builder","A builder structure to create a new gzip Encoder.This structure controls header configuration options such as the filename."],["DecoderReader","A gzip streaming decoderThis structure exposes a `Read` interface that will consume compressed data from the underlying reader and emit uncompressed data."],["EncoderReader","A gzip streaming encoderThis structure exposes a `Read` interface that will read uncompressed data from the underlying reader and expose the compressed version as a `Read` interface."],["EncoderWriter","A gzip streaming encoderThis structure exposes a `Write` interface that will emit compressed data to the underlying writer `W`."],["Header","A structure representing the header of a gzip stream.The header can contain metadata about the file that was compressed, if present."]]});