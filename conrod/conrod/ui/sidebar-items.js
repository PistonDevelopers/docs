initSidebarItems({"enum":[["UiId","For functions that may take either a WidgetId or a CanvasId."]],"struct":[["GlyphCache","A wrapper over some CharacterCache, exposing it's functionality via a RefCell."],["Ui","`Ui` is the most important type within Conrod and is necessary for rendering and maintaining widget state. # Ui Handles the following: * Contains the state of all widgets which can be indexed via their UiId. * Stores rendering state for each widget until the end of each render cycle. * Contains the theme used for default styling of the widgets. * Maintains the latest user input state (for mouse and keyboard). * Maintains the latest window dimensions."],["UserInput","A wrapper over the current user input state."]]});