initSidebarItems({"struct":[["GlobalInput","Global input event handler that also implements `InputProvider`. The `Ui` passes all events to it's `GlobalInput` instance, which aggregates and interprets the events to provide so-called 'high-level' events to widgets. This input gets reset after every update by the `Ui`."]],"type":[["GlobalInputEventIterator","Iterator over global `UiEvent`s. Unlike the `WidgetInputEventIterator`, this will never filter out any events, and all coordinates will be reative to the (0,0) origin of the window."]]});