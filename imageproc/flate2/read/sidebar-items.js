initSidebarItems({"struct":[["DeflateDecoder","A DEFLATE decoder, or decompressor.This structure implements a `Read` interface and takes a stream of compressed data as input, providing the decompressed data when read from."],["DeflateEncoder","A DEFLATE encoder, or compressor.This structure implements a `Read` interface and will read uncompressed data from an underlying stream and emit a stream of compressed data."],["GzDecoder","A gzip streaming decoderThis structure exposes a `Read` interface that will consume compressed data from the underlying reader and emit uncompressed data."],["GzEncoder","A gzip streaming encoderThis structure exposes a `Read` interface that will read uncompressed data from the underlying reader and expose the compressed version as a `Read` interface."],["ZlibDecoder","A ZLIB decoder, or decompressor.This structure implements a `Read` interface and takes a stream of compressed data as input, providing the decompressed data when read from."],["ZlibEncoder","A ZLIB encoder, or compressor.This structure implements a `Read` interface and will read uncompressed data from an underlying stream and emit a stream of compressed data."]]});