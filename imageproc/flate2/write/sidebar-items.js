initSidebarItems({"struct":[["DeflateDecoder","A DEFLATE decoder, or decompressor.This structure implements a `Write` and will emit a stream of decompressed data when fed a stream of compressed data."],["DeflateEncoder","A DEFLATE encoder, or compressor.This structure implements a `Write` interface and takes a stream of uncompressed data, writing the compressed data to the wrapped writer."],["GzEncoder","A gzip streaming encoderThis structure exposes a `Write` interface that will emit compressed data to the underlying writer `W`."],["ZlibDecoder","A ZLIB decoder, or decompressor.This structure implements a `Write` and will emit a stream of decompressed data when fed a stream of compressed data."],["ZlibEncoder","A ZLIB encoder, or compressor.This structure implements a `Write` interface and takes a stream of uncompressed data, writing the compressed data to the wrapped writer."]]});