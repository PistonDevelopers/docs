initSidebarItems({"fn":[["column_running_sum","Computes the running sum of one column of image, padded at the top and bottom. The padding is by continuity. Takes a reference to buffer so that this can be reused for all columns in an image."],["integral_image","Compute the 2d running sum of a grayscale image."],["row_running_sum","Computes the running sum of one row of image, padded at the beginning and end. The padding is by continuity. Takes a reference to buffer so that this can be reused for all rows in an image."],["sum_image_pixels","Sums the pixels in positions [left, right] * [top, bottom] in F, where `integral_image` is the integral image of F."]]});