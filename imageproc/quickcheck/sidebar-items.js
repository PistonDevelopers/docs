initSidebarItems({"fn":[["empty_shrinker","Creates a shrinker with zero elements."],["quickcheck","Convenience function for running QuickCheck.This is an alias for `QuickCheck::new().quickcheck(f)`."],["single_shrinker","Creates a shrinker with a single element."]],"struct":[["QuickCheck","The main QuickCheck type for setting configuration and running QuickCheck."],["StdGen","StdGen is the default implementation of `Gen`.Values of type `StdGen` can be created with the `gen` function in this crate."],["TestResult","Describes the status of a single instance of a test.All testable things must be capable of producing a `TestResult`."]],"trait":[["Arbitrary","`Arbitrary` describes types whose values can be randomly generated and shrunk.Aside from shrinking, `Arbitrary` is different from the `std::Rand` trait in that it uses a `Gen` to control the distribution of random values.As of now, all types that implement `Arbitrary` must also implement `Clone`. (I'm not sure if this is a permanent restriction.)They must also be sendable and static since every test is run in its own thread using `thread::Builder::spawn`, which requires the `Send + 'static` bounds."],["Gen","`Gen` wraps a `rand::Rng` with parameters to control the distribution of random values.A value with type satisfying the `Gen` trait can be constructed with the `gen` function in this crate."],["Rng","A random number generator."],["Testable","`Testable` describes types (e.g., a function) whose values can be tested.Anything that can be tested must be capable of producing a `TestResult` given a random number generator. This is trivial for types like `bool`, which are just converted to either a passing or failing test result.For functions, an implementation must generate random arguments and potentially shrink those arguments if they produce a failure.It's unlikely that you'll have to implement this trait yourself."]]});